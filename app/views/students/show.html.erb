<p id="notice"><%= notice %></p>

<p>
  <strong>First name:</strong>
  <%= @student.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @student.last_name %>
</p>

<table>
<tbody>
<% @courses.each do |course| %>
    <td><%= link_to course.course_name, [@student, course] %></td>
<% end %>
</tbody>
  <%= link_to 'Add New Course', new_student_course_path(@student) %>
  <p></p>
</table>

<%= link_to 'Edit Student Record', edit_student_path(@student) %> |
<%= link_to 'Back to Student Record', students_path %>
